import React from 'react';
import UI from './ui';

export default class App extends React.Component {
    render() {
        return (
            <div>
                <UI/>
            </div>
        );
    }
}